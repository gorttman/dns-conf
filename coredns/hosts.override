# Real A records for i3sec.com.au now live in home.server, in a
# separate i3sec.com.au:53 server block (the main .:53 block already
# declares a `hosts` plugin for k3s's NodeHosts, and CoreDNS only
# allows one `hosts` plugin per server block).
