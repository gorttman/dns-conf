apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kube-system

configMapGenerator:
  - name: coredns-custom
    behavior: create
    files:
      - _placeholder.server=fragments/_placeholder.server
      - hosts.override=hosts.override
generatorOptions:
  disableNameSuffixHash: true
